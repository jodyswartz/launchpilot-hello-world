Implemented the subtle workflow pulse in [index.html](/workspaces/task-23/repo/index.html:116). It adds a small green pulse that travels along the timeline connectors from step to step, with desktop and mobile positioning handled separately and no markup or layout changes.

Verification: `git diff --check` passes.