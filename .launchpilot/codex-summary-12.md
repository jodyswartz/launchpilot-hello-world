Updated [README.md](/workspaces/task-12/repo/README.md) with a concise LaunchPilot overview plus the requested `How it works`, `Example workflow`, and `Requirements` sections.

No secrets, env files, SSH keys, deployment tokens, or unrelated files were edited. The existing untracked `.launchpilot` task files were left untouched.