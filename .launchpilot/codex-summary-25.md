Implemented the `Repos and websites` section polish in [index.html](/workspaces/task-25/repo/index.html:320).

Changed only `index.html`:
- Added simple inline SVG icons to the three cards.
- Updated card titles to `Connected repo`, `Pull request workflow`, and `Live website`.
- Tightened the copy to cover connected GitHub repos, PR review, and opening Vercel/GitHub Pages after merge.
- Kept the existing responsive three-column/mobile stack behavior.

Verification: `git diff --check -- index.html` passed. Untracked `.launchpilot` task files were left untouched.