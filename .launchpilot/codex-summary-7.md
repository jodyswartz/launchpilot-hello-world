`README.md` already contains the requested short section:

```md
## Built with LaunchPilot

This project was built with LaunchPilot.
```

No edits were needed. I left the existing untracked `.launchpilot` files untouched.