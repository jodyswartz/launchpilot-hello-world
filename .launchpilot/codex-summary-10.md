`index.html` already contains the requested “How it works” section with:

1. Telegram idea
2. Codex build
3. GitHub PR

It keeps the existing hero content and includes a mobile-friendly breakpoint that stacks the steps on small screens. I did not make any edits because the smallest useful change here is no change. Current `index.html` has no git diff.