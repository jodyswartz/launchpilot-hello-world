Updated [README.md](/workspaces/task-14/repo/README.md) with the requested beginner-friendly sections:

- `What is LaunchPilot?`
- `How it works`
- `Example workflow`
- `Deployment links`

The README now includes the five-step flow, example `/new`, `/repo`, `/run`, and `/status` commands, and explains that LaunchPilot can show Vercel or GitHub Pages links after merge.

No secrets, env files, SSH keys, deployment tokens, or unrelated files were edited. This was documentation-only, so I did not run tests.