Implemented the three-step “How it works” section in [index.html](/workspaces/task-9/repo/index.html:45).

Added:
- Telegram idea
- Codex build
- GitHub PR

Kept the change scoped to `index.html`. Existing untracked `.launchpilot` task files were left untouched. No tests were run; this is a static HTML/CSS-only change.